{% extends "base.twig" %}

{% block title %}Workload{% endblock %}
{% block body %}
<div class="container">
    <ul class="list-group">
        {% for workload in workloads %}
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    {{ workload.name}}
                    <span class="badge badge-primary badge-pill">{{ workload.COUNT }}</span>
                </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}